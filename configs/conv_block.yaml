model:
  name: ConvBlock
  blocks:
    - id: ConvLayer
      type: conv_layer_basic
      params:
        in_channels: 3
        out_channels: 4
        kernel_size: 3
        stride: 1
        padding: 1
        activation: ReLU
        batch_norm: True
    - id: ConvLayer2
      type: conv_layer_basic
      params:
        in_channels: 4
        out_channels: 8
        kernel_size: 5
        stride: 1
        padding: 2
        activation: LeakyReLU
        batch_norm: True
    - id: ConvLayerFreq
      type: conv_layer_squish_freq
      params:
        in_channels: 11
        out_channels: 4
        kernel_size: (5, 3)
        stride: (4, 1)
        padding: (2, 1)
        activation: ReLU
        batch_norm: True
    - id: ConvLayerTime
      type: conv_layer_squish_time
      params:
        in_channels: 11
        out_channels: 4
        kernel_size: (3, 5)
        stride: (1, 4)
        padding: (1, 2)
        activation: LeakyReLU
        batch_norm: True
    - id: stacked_conv
      type: conv_layer_stacked
      params:
        in_channels: 8
        out_channels: 1
        kernel_size: 1
        stride: 1
        padding: 0
        activation: LeakyReLU
        batch_norm: False
    